cmake_minimum_required(VERSION 3.15)
project(hccl_test)

message(${CMAKE_CURRENT_SOURCE_DIR})
set(HCCL_TEST_DIR ${CMAKE_CURRENT_SOURCE_DIR})

install(DIRECTORY
    ${HCCL_TEST_DIR}
    DESTINATION ${INSTALL_LIBRARY_DIR} OPTIONAL
)